# Hyperkit

## Overview

TODO

## Requirements

Use [Deno](https://deno.com) version 2.5 or higher.

## Tasks

- `deno run bundle` bundles the client assets.
- `deno run serve` starts the file server.
